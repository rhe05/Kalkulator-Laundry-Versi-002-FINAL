/** File pendek khusus supaya fungsi tes ada di baris paling atas (mudah dipilih & Run dari editor, tanpa perlu scroll). Aman dihapus kapan saja setelah tes selesai. */
function jalankanTesKoneksiFirestore_() {
  return testFirestoreConnection_();
}
